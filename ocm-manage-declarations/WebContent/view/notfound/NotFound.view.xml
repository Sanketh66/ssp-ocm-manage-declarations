<mvc:View controllerName="com.ssp.ocm.manage.declarations.notfound.NotFound"
    displayBlock="true"
    busy="{appView>/busy}"
    busyIndicatorDelay="{appView>/delay}"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
    <Page class="notfoundPage globalPage"
        enableScrolling="false"
        floatingFooter="true"
        showNavButton="false"
        showFooter="false"
        title="{i18n>notfoundNotFoundTitle}">
        <headerContent>
        </headerContent>
        <content>
        </content>
    </Page>
</mvc:View>
